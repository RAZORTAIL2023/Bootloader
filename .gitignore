# ================================================
# .gitignore 文件 - Git 版本控制忽略规则
# ================================================
# 此文件告诉 Git 哪些文件和目录应该被忽略，不纳入版本控制
# 语法规则：
# - 以 # 开头的行是注释
# - 以 / 结尾的是目录
# - 以 / 开头的是相对于根目录的路径
# - 使用 * 匹配任意字符（除了 /）
# - 使用 ** 匹配任意数量的目录
# - 使用 ! 取消忽略（白名单）
# - 使用 ? 匹配单个字符

# C/C++
*.o               # 目标文件
*.obj             # Windows 目标文件
*.exe             # Windows 可执行文件
*.dll             # Windows 动态链接库
*.so              # Linux/Unix 共享库
*.dylib           # macOS 动态库
*.a               # 静态库文件
*.lib             # Windows 静态库
*.pdb             # Windows 程序数据库文件
*.ilk             # 增量链接文件
*.exp             # 导出文件
*.gch             # 预编译头文件
*.pch             # 预编译头文件

# 编译器生成的文件
*.out             # 默认输出文件
*.app             # macOS 应用程序包
*.stackdump       # Cygwin 堆栈转储文件
*.dSYM/           # macOS 调试符号文件夹

# 构建目录
build/            # 构建输出目录
Build/            # 构建输出目录（大写）
debug/            # 调试构建目录
Debug/            # 调试构建目录（大写）
release/          # 发布构建目录
Release/          # 发布构建目录（大写）
cmake-build-*/    # CMake 构建目录
